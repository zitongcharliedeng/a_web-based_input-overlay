name: Deploy Experimental Branch

on:
  push:
    branches:
      - 'claude/experimental-01GVQNvaEXT11NUP4p9Vdo3q'

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  deploy-experimental:
    uses: ./.github/workflows/build-web.yml
    permissions:
      contents: write
      pages: write
      id-token: write
    with:
      deploy-path: 'experimental'

# Tauri Icons

This directory should contain app icons for Tauri.

For a minimal demo, you can use Tauri's default icons or create your own:

```bash
# Generate icons from a source image (if you have one)
cargo tauri icon path/to/icon.png
```

For now, the app will use default icons or skip icon generation.

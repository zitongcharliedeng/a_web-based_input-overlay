{
  "name": "web-based-input-overlay-webapp",
  "version": "1.0.0",
  "description": "Web application for input overlay visualization",
  "scripts": {
    "build": "tsc && tsc browserInputListeners/mouse.ts --outDir browserInputListeners/_compiled --target ES2020 --module ES2020 --lib ES2020,DOM --skipLibCheck",
    "serve": "http-server -p 8080 -c-1 -o",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "http-server": "^14.1.1"
  }
}

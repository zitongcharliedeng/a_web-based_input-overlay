{
  "name": "web-based-input-overlay-webapp",
  "version": "1.0.0",
  "type": "module",
  "description": "Web application for input overlay visualization",
  "scripts": {
    "prebuild": "tsx _helpers/generateVersionGitCommitHash.ts && rimraf sceneRender/_compiled inputListeners/_compiled",
    "build": "tsc && tsc inputListeners/mouse.ts --outDir inputListeners/_compiled --target ES2020 --module ES2020 --lib ES2020,DOM --skipLibCheck",
    "serve": "http-server -p 8080 -c-1 -o",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "http-server": "^14.1.1",
    "rimraf": "^6.1.0",
    "tsx": "^4.20.6",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "zod": "^4.1.12"
  }
}

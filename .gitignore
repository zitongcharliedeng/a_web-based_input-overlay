# Claude AI assistant directory (local config only)
.claude/

# Node modules (build artifacts)
node_modules/

# Note: package-lock.json IS tracked (dependency lockfile, not build artifact)
# Ensures reproducible builds with exact dependency versions

# TypeScript compiled output (build artifacts - compile locally)
# Pure source-only approach: .ts files are source of truth
# Exclude compiled JS but NOT config files
**/viewWhichRendersConfigurationAndUi/**/*.js
**/modelToSaveCustomConfigurationLocally/**/*.js
**/controllerToMutateCustomConfiguration/**/*.js
**/_helpers/**/*.js
**/_compiled/**/*.js
**/dist/**/*.js
**/*.js.map

# IMPORTANT: Do NOT ignore config files
!*.config.js
!eslint.config.js

# esbuild bundled output
webApp/dist/

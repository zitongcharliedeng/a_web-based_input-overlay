[package]
name = "raylib-input-overlay-demo"
version = "0.1.0"
edition = "2021"

[dependencies]
raylib = "5.0"
rdev = { version = "0.5.3", features = ["unstable_grab"] }

[profile.release]
opt-level = 3
lto = true

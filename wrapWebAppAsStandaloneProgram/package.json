{
  "name": "web-based-input-overlay-electron",
  "version": "1.0.0",
  "description": "Electron wrapper for web-based input overlay",
  "main": "main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron . --enable-logging --with-dev-console"
  },
  "devDependencies": {
    "electron": "^34.5.8",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@kmamal/sdl": "^0.11.13",
    "uiohook-napi": "^1.5.4"
  }
}

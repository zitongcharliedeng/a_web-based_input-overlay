{
  "name": "analog-keyboard-overlay",
  "version": "1.0.0",
  "description": "Web-based analog keyboard and gamepad overlay for streamers",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "type-check": "tsc --noEmit",
    "format": "prettier --write src/ browserInputListeners/ browserInputOverlayView/"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@tauri-apps/cli": "^1.5.0",
    "@tauri-apps/api": "^1.5.0",
    "vite": "^5.0.0",
    "prettier": "^3.1.0"
  },
  "dependencies": {}
}
